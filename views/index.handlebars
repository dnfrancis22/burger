<img src="/assets/hamburger.png" alt="illustration of a hamburger.">
<h1>Eat-Da-Burger!</h1>

<div class="container">
  <div class="row">
    <div class="col-sm">
      <h2>Burgers to Devour!</h2>
      {{!-- the list of burger names and the devour button will be injected here from the burger-block file --}}
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-sm">

      <h2>Devoured Burgers!</h2>
      {{!-- the list of burger names will be injected th if conditions determine what burgers go here. --}}
      <ul>
        {{#each burgers}}
        {{#if devoured}}
        <li> {{burger_name}}</li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-sm">

      <h2>Add a Burger!</h2>
      {{!-- this is the form that allows you to add a burger. --}}
      <form class="create-form">
        <div class="form-group">
          <label for="bg">Burger Name:</label>
          <input type="text" id="bg" name="burger_name">
          <button type="submit">Add a Burger</button>
        </div>
      </form>
    </div>
  </div>
</div>